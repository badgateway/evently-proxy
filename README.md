Evently Curveball Proxy
=======================

To get started:

* Have a recent Node 16 or higher (a built-in `fetch()` is required).
* `npm i`
* `cp .env.defaults .env`
* Edit `.env` to add your token
* `npm run start:watch`
